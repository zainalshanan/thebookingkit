---
title: AvailabilityEditor
description: Weekly schedule builder with RRULE output.
---

A visual editor for building weekly availability schedules. Outputs RRULE strings for storage in the database.

## Usage

```tsx
import { AvailabilityEditor, scheduleToRRules } from "./components/availability-editor";

function AvailabilityPage() {
  const [schedule, setSchedule] = useState<WeeklySchedule>({
    monday: [{ start: "09:00", end: "17:00" }],
    tuesday: [{ start: "09:00", end: "17:00" }],
    wednesday: [{ start: "09:00", end: "17:00" }],
    thursday: [{ start: "09:00", end: "17:00" }],
    friday: [{ start: "09:00", end: "17:00" }],
    saturday: [],
    sunday: [],
  });

  const handleSave = () => {
    const rrules = scheduleToRRules(schedule, "America/New_York");
    // Save rrules to database
  };

  return (
    <AvailabilityEditor
      schedule={schedule}
      onScheduleChange={setSchedule}
      timezone="America/New_York"
    />
  );
}
```

## Props

```typescript
interface AvailabilityEditorProps {
  /** Current weekly schedule */
  schedule: WeeklySchedule;
  /** Callback when schedule changes */
  onScheduleChange: (schedule: WeeklySchedule) => void;
  /** Provider's timezone */
  timezone: string;
  /** Additional CSS classes */
  className?: string;
}

interface WeeklySchedule {
  monday: TimeRange[];
  tuesday: TimeRange[];
  wednesday: TimeRange[];
  thursday: TimeRange[];
  friday: TimeRange[];
  saturday: TimeRange[];
  sunday: TimeRange[];
}

interface TimeRange {
  start: string; // "HH:mm"
  end: string;   // "HH:mm"
}
```

## Features

- Toggle days on/off
- Add multiple time blocks per day (e.g., morning + afternoon with lunch break)
- Time picker for start/end times
- Copy schedule from one day to others
- `scheduleToRRules()` helper converts the visual schedule to RRULE strings
